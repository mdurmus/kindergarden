@model IEnumerable<kindergarden.Models.gallery>
@{
    ViewBag.Title = "Galleries";
}

<div class="col-xs-12">
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Gallerien</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body table-responsive no-padding">
            <table class="table table-hover">
                <tbody>
                    <tr>
                        <th>Name</th>
                        <th>Prozess</th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.name</td>
                            <td>
                                <a href="@Url.Action("ShowGallery","ParentUser", new {galleryId=item.Id })" class="btn btn-primary">Fotos</a>
                               
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
</div>