@model IEnumerable<Person>

@{
    ViewBag.Title = "List Parents";
}

@if (Model.Count() > 0)
{
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Alle Eltern</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tbody>
                        <tr>
                            <th>Email</th>
                            <th>Name</th>
                            <th>Familienname</th>
                            <th>Telefonnumer</th>
                            <th>Prozess</th>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Email</td>
                                <td>@item.Name</td>
                                <td>@item.LastName</td>
                                <td><a href="tel:@item.Gsm" target="_blank">@item.Gsm</a></td>

                                <td>
                                    <a href="@Url.Action("ApproveParent","AdminUser",new {parentId=item.Id })" onclick="return confirm('Sind Sie sicher?')" class="btn btn-warning">Freischalten</a> |
                                    <a href="@Url.Action("DenyParent","AdminUser",new {parentId=item.Id })" onclick="return confirm('Sind Sie sicher?')" class="btn btn-danger">Nicht Freischalten</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="col-md-12">
        <div class="box box-danger">
            <div class="box-header with-border">
                <h3 class="box-title">Keine Daten!</h3>
            </div>
        </div>
    </div>
}