
@{
    ViewBag.Title = "Send New Message";
}

@*
<div class="col-md-12">
    <div class="box box-danger">
        <div class="box-header with-border">
            <h3 class="box-title">Nachricht Senden</h3>
        </div>
        <div class="box-body">
            <div class="row">
                @using (Html.BeginForm("SendMessage", "AdminUser", FormMethod.Post))
                {
                    <div class="col-md-12">
                        <div class="form-group">
                            <label class="control-label" for="personId">An: </label>
                            @Html.DropDownList("personId", ViewBag.Person as SelectList, new { @class = "selectD form-control" })
                        </div>
                        <div class="form-group">
                            <label class="control-label" for="messageText">Nachticht </label>
                            <textarea name="messageText" rows="4" cols="50" class="form-control"></textarea>
                        </div>
                        <button type="submit" class="btn btn-success btn-block">Nachticht senden</button>
                    </div>
                }
            </div>
        </div>
        <!-- /.box-body -->
    </div>


</div>
*@